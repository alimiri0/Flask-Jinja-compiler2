<style>
  body{font-family: Arial; padding: 20px; }
  ul { padding: 0; }
  li { list-style: none; margin: 6px 0; }
  a { text-decoration: none; color: #007bff; }
  a:hover { text-decoration: underline; }
  .btn { display:inline-block; margin-top:20px; background:#28a745; color:#fff; padding:8px 12px; border-radius:4px; }
</style>

<h1>Products</h1>

<ul>
  {% for p in products %}
    <li>
      <img src="{{ p.image }}">
      <a href="{{ url_for('show', pid=p.id) }}">{{ p.name }}</a>
      {{ p.price }}
      {{ p.details }}
    </li>
  {% endfor %}
</ul>

<a class="btn" href="{{ url_for('create') }}">Create Product</a>